import { ResponseTemplate } from './response-template.model';

describe('ResponseTemplate', () => {
  it('should create an instance', () => {
    expect(new ResponseTemplate()).toBeTruthy();
  });
});
